# Digital TAU

Платформа для демонстрации студенческих проектов Astana Hub.

## 🚀 Быстрый старт (Quick Start)

### Предварительные требования
- Docker и Docker Compose
- 2GB свободной оперативной памяти
- Порты 5432 и 8000 должны быть свободны

### Запуск приложения

```bash
# 1. Клонировать репозиторий
git clone <repository-url>
cd digital-tau

# 2. Создать .env файл
cp .env.example .env

# 3. Запустить контейнеры
docker compose up -d db api

# 4. Проверить статус
docker compose ps
```

### ✅ Быстрая проверка

Запустите скрипт автоматической проверки:

```bash
./verify.sh
```

Этот скрипт проверит:
- ✅ Статус Docker контейнеров
- ✅ Подключение к базе данных
- ✅ Все API endpoints
- ✅ Систему администрирования
- ✅ Инициализацию данных

## 📚 Документация

### Основные файлы
- **VERIFICATION_GUIDE.md** - Полное руководство по проверке и тестированию
- **verify.sh** - Скрипт автоматической проверки
- **.env.example** - Пример конфигурации

### Структура проекта

```
digital-tau/
├── dt_backend/          # Backend приложение (FastAPI)
│   ├── app.py          # Главное приложение
│   ├── config.py       # Конфигурация
│   ├── db.py           # Работа с БД
│   └── routers/        # API endpoints
├── db/                 # Инициализация БД
│   └── init/          # SQL скрипты
├── static/            # Статические файлы
├── templates/         # Jinja2 шаблоны
├── frontend/          # Next.js frontend
├── docker-compose.yml # Docker конфигурация
└── requirements.txt   # Python зависимости
```

## 🔧 Конфигурация

### Переменные окружения (.env)

```env
# База данных PostgreSQL
POSTGRES_USER=dt_user
POSTGRES_PASSWORD=dt_pass
POSTGRES_DB=dt_db
POSTGRES_PORT=5432

# Backend API
API_PORT=8000
ADMIN_USER=admin
ADMIN_PASS=admin123
SECRET_KEY=super-secret-change-me

# Frontend
FRONTEND_PORT=3000
FRONTEND_URL=http://localhost:3000

# CORS
CORS_ORIGINS=http://localhost:3000,http://localhost:3001
```

⚠️ **Важно:** Измените учетные данные администратора и SECRET_KEY перед развертыванием в продакшен!

## 🌐 API Endpoints

### Публичные endpoints

```bash
# Проверка здоровья
GET /health
# Response: {"status": "ok"}

# Статистика
GET /api/stats
# Response: {"projects": 1, "students": 500, "technologies": 3}

# Список проектов
GET /api/projects
GET /api/projects?category=web

# Категории
GET /api/categories

# Технологии
GET /api/technologies
```

### Администрирование

```bash
# Вход в админ-панель
GET /api/admin/login

# Управление проектами
GET /api/admin/projects

# Управление категориями
GET /api/admin/categories

# Управление технологиями
GET /api/admin/technologies
```

**Учетные данные по умолчанию:**
- Username: `admin`
- Password: `admin123`

## 🗄️ База данных

### Схема

```sql
-- Проекты
projects (
  id, title_ru, title_kz, title_en,
  description_ru, description_kz, description_en,
  technologies[], genres[], categories[],
  image, images[], category, featured,
  project_url
)

-- Технологии
technologies (id, name)

-- Категории
categories (id, name, name_ru, name_kz, name_en)

-- Жанры
genres (id, name)
```

### Доступ к БД

```bash
# Подключиться к PostgreSQL
docker exec -it dt_db psql -U dt_user -d dt_db

# Примеры запросов
SELECT * FROM categories;
SELECT * FROM projects;
SELECT * FROM technologies;
```

## 🛠️ Разработка

### Запуск в режиме разработки

```bash
# Запустить только базу данных
docker compose up -d db

# Установить зависимости Python
pip install -r requirements.txt

# Запустить backend локально с hot reload
uvicorn main:app --reload --host 0.0.0.0 --port 8000
```

### Логи

```bash
# Просмотр логов всех контейнеров
docker compose logs -f

# Логи только API
docker compose logs -f api

# Логи только БД
docker compose logs -f db
```

### Остановка

```bash
# Остановить контейнеры
docker compose down

# Остановить и удалить данные
docker compose down -v
```

## 🧪 Тестирование

### Автоматические тесты

```bash
# Запустить полный набор тестов
./verify.sh

# Тесты через curl
curl http://localhost:8000/health
curl http://localhost:8000/api/stats
curl http://localhost:8000/api/projects
curl http://localhost:8000/api/categories
```

### Ручное тестирование

1. Откройте http://localhost:8000/api/admin/login
2. Войдите с учетными данными: admin / admin123
3. Проверьте управление проектами
4. Добавьте тестовый проект
5. Проверьте API: http://localhost:8000/api/projects

## 📊 Стек технологий

### Backend
- **FastAPI** - современный веб-фреймворк
- **SQLAlchemy** - ORM для работы с БД
- **Psycopg** - PostgreSQL адаптер
- **Jinja2** - шаблонизатор
- **Uvicorn** - ASGI сервер

### Database
- **PostgreSQL 16** - реляционная база данных

### Frontend
- **Next.js 16** - React фреймворк
- **TypeScript** - типизированный JavaScript
- **Tailwind CSS** - utility-first CSS

### Инфраструктура
- **Docker** - контейнеризация
- **Docker Compose** - оркестрация контейнеров
- **Nginx** - reverse proxy (опционально)

## 🔒 Безопасность

### Чеклист перед продакшеном

- [ ] Изменить ADMIN_USER и ADMIN_PASS
- [ ] Сгенерировать надежный SECRET_KEY
- [ ] Настроить CORS_ORIGINS
- [ ] Включить HTTPS/SSL
- [ ] Настроить firewall правила
- [ ] Включить rate limiting
- [ ] Настроить мониторинг
- [ ] Настроить резервное копирование БД
- [ ] Проверить на SQL injection
- [ ] Проверить на XSS уязвимости

## 📖 Дополнительная документация

- [VERIFICATION_GUIDE.md](./VERIFICATION_GUIDE.md) - Полное руководство по проверке
- [docker-compose.yml](./docker-compose.yml) - Конфигурация контейнеров
- [.env.example](./.env.example) - Пример переменных окружения

## 🤝 Поддержка

При возникновении проблем:

1. Проверьте логи: `docker compose logs -f`
2. Запустите проверку: `./verify.sh`
3. Прочитайте [VERIFICATION_GUIDE.md](./VERIFICATION_GUIDE.md)
4. Проверьте порты: `netstat -tulpn | grep -E '(5432|8000)'`

## 📝 Лицензия

Copyright © 2026 Astana Hub

## ✅ Статус проверки

Последняя проверка: **2026-02-13**

- ✅ База данных: PostgreSQL 16 работает
- ✅ API Backend: FastAPI отвечает корректно
- ✅ Endpoints: Все публичные API работают
- ✅ Админ-панель: Доступна и функциональна
- ✅ Многоязычность: Поддержка ru/kz/en
- ⚠️ Frontend: Требует исправления npm зависимостей

**Приложение готово к разработке и тестированию!** 🚀
