# Как запустить и проверить полный сайт Digital TAU

## 🚀 Быстрый запуск полного сайта

### Шаг 1: Подготовка

Убедитесь, что у вас установлены:
- Docker и Docker Compose
- Свободно минимум 3GB RAM
- Свободны порты: 80, 3000, 5432, 8000

### Шаг 2: Создайте файл конфигурации

```bash
# Скопируйте пример конфигурации
cp .env.example .env

# Отредактируйте .env при необходимости (по умолчанию всё работает)
```

### Шаг 3: Запустите все сервисы

```bash
# Запуск ВСЕХ компонентов (база данных, бэкенд API, фронтенд, nginx)
docker compose up -d

# Проверьте статус всех контейнеров
docker compose ps
```

Вы должны увидеть 4 запущенных контейнера:
- `dt_db` (PostgreSQL)
- `dt_api` (FastAPI backend)
- `dt_frontend` (Next.js frontend)
- `dt_nginx` (Nginx proxy)

### Шаг 4: Дождитесь запуска всех сервисов

```bash
# Подождите ~30-60 секунд для полной инициализации
# Следите за логами
docker compose logs -f

# Или проверьте отдельные сервисы
docker compose logs frontend
docker compose logs api
```

### Шаг 5: Откройте сайт

После запуска всех сервисов, откройте в браузере:

🌐 **Полный сайт:** http://localhost

---

## 📍 Доступные URL адреса

### Основной сайт (через Nginx)
```
http://localhost           - Главная страница (Next.js frontend)
http://localhost/projects  - Страница проектов
http://localhost/about     - О нас
```

### Backend API (прямой доступ)
```
http://localhost:8000/health        - Проверка здоровья API
http://localhost:8000/api/stats     - Статистика
http://localhost:8000/api/projects  - Список проектов (JSON)
http://localhost:8000/api/categories - Категории
```

### ⭐ Админ-панель (оригинальный интерфейс - ВОССТАНОВЛЕН)
```
http://localhost:8000/admin/login    - Вход в админку (шаблоны Jinja2)
http://localhost:8000/admin/projects - Управление проектами

Учётные данные по умолчанию:
  Логин: admin
  Пароль: admin123
```

### Frontend напрямую (без Nginx)
```
http://localhost:3000      - Next.js приложение напрямую
```

---

## 🔍 Проверка работоспособности

### Автоматическая проверка

```bash
# Запустите скрипт автоматической проверки
./verify.sh
```

Этот скрипт проверит:
- ✅ Docker контейнеры
- ✅ Базу данных
- ✅ API endpoints
- ✅ Админ-панель
- ✅ Инициализацию данных

### Ручная проверка

#### 1. Проверьте базу данных
```bash
# Подключитесь к PostgreSQL
docker exec -it dt_db psql -U dt_user -d dt_db

# Проверьте таблицы
\dt

# Проверьте категории
SELECT * FROM categories;

# Выйдите
\q
```

#### 2. Проверьте Backend API
```bash
# Проверка здоровья
curl http://localhost:8000/health

# Получить статистику
curl http://localhost:8000/api/stats

# Список проектов
curl http://localhost:8000/api/projects
```

#### 3. Проверьте админ-панель

Откройте в браузере:
1. http://localhost:8000/admin/login
2. Войдите с учётными данными: admin / admin123
3. Перейдите к управлению проектами
4. Попробуйте добавить тестовый проект

#### 4. Проверьте фронтенд

Откройте в браузере:
1. http://localhost - главная страница
2. Проверьте отображение проектов
3. Проверьте переключение языков (RU/KZ/EN)
4. Проверьте фильтрацию по категориям

---

## 🛠️ Команды управления

### Просмотр логов

```bash
# Все логи
docker compose logs -f

# Только frontend
docker compose logs -f frontend

# Только API
docker compose logs -f api

# Только база данных
docker compose logs -f db
```

### Перезапуск сервисов

```bash
# Перезапустить все
docker compose restart

# Перезапустить только frontend
docker compose restart frontend

# Перезапустить только API
docker compose restart api
```

### Остановка и очистка

```bash
# Остановить все сервисы
docker compose down

# Остановить и удалить данные БД
docker compose down -v
```

### Пересборка при изменениях

```bash
# Пересобрать и запустить все
docker compose up -d --build

# Пересобрать только API
docker compose up -d --build api

# Пересобрать только frontend
docker compose up -d --build frontend
```

---

## 📊 Структура сервисов

```
┌─────────────────────────────────────────────┐
│   Браузер пользователя                       │
└──────────────┬──────────────────────────────┘
               │ http://localhost
               ▼
┌─────────────────────────────────────────────┐
│   Nginx (порт 80)                            │
│   - Reverse proxy                            │
│   - Маршрутизация запросов                   │
└──────┬──────────────────────┬────────────────┘
       │                      │
       │ /api, /static        │ /, /admin (frontend)
       ▼                      ▼
┌──────────────────┐   ┌──────────────────────┐
│  Backend API     │   │  Next.js Frontend    │
│  FastAPI         │   │  React               │
│  (порт 8000)     │   │  (порт 3000)         │
│                  │   │                      │
│  - API endpoints │   │  - Главная страница  │
│  - Админка       │   │  - Страницы проектов │
│  - Статика       │   │  - UI компоненты     │
└────────┬─────────┘   └──────────────────────┘
         │
         │ DATABASE_URL
         ▼
┌──────────────────────────────────────────────┐
│   PostgreSQL (порт 5432)                     │
│   - Проекты                                  │
│   - Технологии                               │
│   - Категории                                │
└──────────────────────────────────────────────┘
```

---

## ⚠️ Возможные проблемы и решения

### Frontend не запускается

**Проблема:** `npm ci` ошибка или `next: not found`

**Решение:**
```bash
# Очистите кеш npm в контейнере frontend
docker compose exec frontend rm -rf node_modules
docker compose exec frontend rm -f package-lock.json

# Пересоберите frontend
docker compose up -d --build frontend
```

### Порт уже занят

**Проблема:** `port is already allocated`

**Решение:**
```bash
# Проверьте какие порты используются
sudo netstat -tulpn | grep -E ':(80|3000|5432|8000)'

# Остановите конфликтующие сервисы или измените порты в .env
```

### База данных не инициализируется

**Проблема:** Таблицы не создаются

**Решение:**
```bash
# Удалите данные и пересоздайте
docker compose down -v
docker compose up -d
```

### Админ-панель показывает 404

**Проблема:** Не можете открыть /admin/login

**Решение:**
Используйте прямой доступ к backend:
- http://localhost:8000/admin/login (оригинальная админка)

Через Nginx (порт 80), `/admin` идёт на frontend.

### API возвращает ошибки подключения к БД

**Проблема:** `connection failed`

**Решение:**
```bash
# Проверьте, что база данных запущена
docker compose ps db

# Посмотрите логи API
docker compose logs api

# Убедитесь что DATABASE_URL правильный в .env
```

---

## 🎯 Тестирование полного функционала

### 1. Тест главной страницы
- [ ] Открыть http://localhost
- [ ] Проверить загрузку страницы
- [ ] Проверить меню навигации
- [ ] Проверить отображение контента

### 2. Тест проектов
- [ ] Открыть страницу проектов
- [ ] Проверить фильтрацию по категориям
- [ ] Проверить переключение языков (RU/KZ/EN)
- [ ] Проверить карточки проектов

### 3. Тест API
- [ ] GET /api/stats - получить статистику
- [ ] GET /api/projects - получить список проектов
- [ ] GET /api/categories - получить категории
- [ ] GET /api/technologies - получить технологии

### 4. Тест админ-панели
- [ ] Открыть http://localhost:8000/admin/login
- [ ] Войти с admin/admin123
- [ ] Просмотреть список проектов
- [ ] Добавить новый проект
- [ ] Редактировать проект
- [ ] Удалить тестовый проект

### 5. Тест базы данных
- [ ] Подключиться через psql
- [ ] Проверить таблицы
- [ ] Проверить данные в categories
- [ ] Проверить созданные проекты

---

## 📝 Примеры команд для быстрого теста

```bash
# Полный цикл тестирования
cd /path/to/digital-tau

# 1. Запуск
docker compose up -d

# 2. Ждём 60 секунд
sleep 60

# 3. Проверка здоровья
curl http://localhost:8000/health

# 4. Проверка API
curl http://localhost:8000/api/stats
curl http://localhost:8000/api/projects

# 5. Проверка админки (откройте в браузере)
# http://localhost:8000/admin/login

# 6. Проверка frontend (откройте в браузере)
# http://localhost

# 7. Просмотр логов
docker compose logs --tail=50

# 8. Остановка
docker compose down
```

---

## 💡 Советы

1. **Первый запуск может занять 2-3 минуты** для сборки образов и инициализации БД

2. **Frontend может долго собираться** из-за npm зависимостей. Следите за логами:
   ```bash
   docker compose logs -f frontend
   ```

3. **Для разработки** можно запускать только нужные сервисы:
   ```bash
   # Только бэкенд и БД
   docker compose up -d db api
   
   # Только фронтенд и БД
   docker compose up -d db frontend
   ```

4. **Используйте .env для настройки** портов и других параметров

5. **Две админ-панели:**
   - Backend (восстановленная): http://localhost:8000/admin/login
   - Frontend (новая): http://localhost/admin (через Nginx)

---

## 🔗 Полезные ссылки

- **README.md** - Основная документация
- **VERIFICATION_GUIDE.md** - Детальное руководство по проверке
- **verify.sh** - Автоматический скрипт проверки
- **.env.example** - Пример конфигурации

---

## ✅ Итоговый чек-лист запуска

- [ ] Скопировали .env файл (`cp .env.example .env`)
- [ ] Запустили все сервисы (`docker compose up -d`)
- [ ] Дождались запуска (~60 секунд)
- [ ] Проверили статус (`docker compose ps` - все 4 контейнера UP)
- [ ] Открыли сайт в браузере (http://localhost)
- [ ] Проверили админку (http://localhost:8000/admin/login)
- [ ] Проверили API (http://localhost:8000/api/stats)
- [ ] Всё работает! 🎉

---

**Если что-то не работает** - смотрите раздел "Возможные проблемы и решения" выше или проверьте логи:
```bash
docker compose logs
```
