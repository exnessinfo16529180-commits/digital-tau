# 🚀 БЫСТРЫЙ СТАРТ - Для начинающих

**Простая инструкция как запустить сайт Digital TAU**

---

## ⚡ Самый быстрый способ (3 шага)

### Шаг 1: Установите Docker

**Если Docker уже установлен - пропустите этот шаг!**

#### Для Windows:
1. Скачайте Docker Desktop: https://www.docker.com/products/docker-desktop
2. Запустите установщик
3. Перезагрузите компьютер
4. Откройте Docker Desktop

#### Для Mac:
1. Скачайте Docker Desktop: https://www.docker.com/products/docker-desktop
2. Перетащите Docker в папку Applications
3. Запустите Docker из Applications

#### Для Linux (Ubuntu/Debian):
```bash
curl -fsSL https://get.docker.com -o get-docker.sh
sudo sh get-docker.sh
```

**Проверьте что Docker работает:**
```bash
docker --version
```

Должно показать что-то вроде: `Docker version 24.0.0`

---

### Шаг 2: Скачайте проект

**Откройте терминал (командную строку) и выполните:**

```bash
# Перейдите в папку где хотите сохранить проект
cd ~/Desktop

# Скачайте проект
git clone https://github.com/exnessinfo16529180-commits/digital-tau.git

# Перейдите в папку проекта
cd digital-tau
```

**Если у вас нет git:**
- Windows: Скачайте с https://git-scm.com/download/win
- Mac: Установится автоматически при первой попытке использовать git
- Linux: `sudo apt-get install git`

---

### Шаг 3: Запустите сайт

**Просто выполните одну команду:**

```bash
./start.sh
```

**Вот и всё! 🎉**

Скрипт автоматически:
- ✅ Настроит всё необходимое
- ✅ Загрузит и запустит все компоненты
- ✅ Покажет вам когда всё готово

**Подождите 1-2 минуты** пока всё запускается (при первом запуске может быть дольше).

---

## 🌐 Откройте сайт

Когда скрипт завершится, откройте в браузере:

### 🏠 Главная страница:
```
http://localhost:8000
```

### 👤 Админ-панель:
```
http://localhost:8000/admin/login

Логин: admin
Пароль: admin123
```

---

## ❓ Что-то пошло не так?

### Проблема 1: "command not found: ./start.sh"

**Решение:**
```bash
# Сделайте скрипт исполняемым
chmod +x start.sh

# Попробуйте снова
./start.sh
```

### Проблема 2: "docker: command not found"

**Решение:**
- Docker не установлен или не запущен
- Вернитесь к Шагу 1 и установите Docker
- Убедитесь что Docker Desktop запущен (должна быть иконка в трее)

### Проблема 3: "port is already allocated"

**Решение:**
```bash
# Остановите существующие контейнеры
docker compose down

# Попробуйте снова
./start.sh
```

### Проблема 4: Браузер показывает "Не удается получить доступ к сайту"

**Решение:**
1. Подождите ещё минуту - сайт может всё ещё запускаться
2. Проверьте что контейнеры работают:
   ```bash
   docker compose ps
   ```
3. Посмотрите логи:
   ```bash
   docker compose logs
   ```

---

## 🛑 Как остановить сайт

Когда закончите работу:

```bash
docker compose down
```

---

## 📝 Что происходит при запуске?

Когда вы запускаете `./start.sh`, автоматически:

1. **Создаётся конфигурация** (.env файл)
2. **Запускается база данных** (PostgreSQL)
3. **Запускается backend** (FastAPI - это API сервер)
4. **Запускается frontend** (Next.js - это веб-интерфейс)
5. **Настраивается прокси** (Nginx - распределяет запросы)

Всё это делается автоматически!

---

## 🔄 Перезапуск

Если нужно перезапустить:

```bash
# Остановить
docker compose down

# Запустить снова
./start.sh
```

---

## 💡 Полезные команды

### Посмотреть что работает:
```bash
docker compose ps
```

### Посмотреть логи (что происходит):
```bash
docker compose logs -f
```

### Остановить всё:
```bash
docker compose down
```

### Удалить всё и начать с нуля:
```bash
docker compose down -v
./start.sh
```

---

## 📖 Нужна дополнительная информация?

Если этот гайд слишком простой и вам нужно больше деталей:

- **RUNNING_FULL_SITE.md** - Подробное руководство
- **README.md** - Техническая документация
- **verify.sh** - Скрипт для проверки что всё работает

---

## ✅ Чек-лист быстрого старта

- [ ] Docker установлен и запущен
- [ ] Проект скачан (`git clone ...`)
- [ ] Перешли в папку проекта (`cd digital-tau`)
- [ ] Запустили скрипт (`./start.sh`)
- [ ] Подождали 1-2 минуты
- [ ] Открыли http://localhost:8000 в браузере
- [ ] Вижу сайт! 🎉

---

## 🎓 Для совсем начинающих

### Что такое терминал/командная строка?

**Windows:**
- Нажмите `Win + R`
- Введите `cmd` или `powershell`
- Нажмите Enter

**Mac:**
- Нажмите `Cmd + Space`
- Введите `Terminal`
- Нажмите Enter

**Linux:**
- Нажмите `Ctrl + Alt + T`

### Как копировать команды?

1. Выделите команду мышкой
2. Нажмите `Ctrl + C` (или `Cmd + C` на Mac)
3. В терминале: щёлкните правой кнопкой мыши → Вставить
4. Нажмите Enter

### Что такое Docker?

Docker - это программа которая запускает приложения в "контейнерах". 
Контейнер - это как отдельный мини-компьютер для приложения.
Вам не нужно понимать как это работает - просто установите Docker и всё! 😊

---

## 🆘 Всё равно не получается?

1. Убедитесь что Docker Desktop **запущен** (должна быть иконка в трее/панели)
2. Попробуйте **перезагрузить компьютер** и запустить снова
3. Проверьте что у вас **достаточно места** на диске (нужно минимум 5GB)
4. Проверьте что **порты свободны** (закройте другие веб-серверы)

---

**Успехов! Если всё получилось - можете начинать работать с сайтом! 🚀**
